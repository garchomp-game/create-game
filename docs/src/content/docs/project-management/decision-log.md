---
title: 意思決定記録
description: 主要な方針変更と、その判断理由。
---

## 2026-06-29: v0.3安定版候補

決定: v0.3を安定版候補として扱う。

理由:

- 最終検証が通った。
- 回復ピックアップはゲームを壊していない。
- 画面外敵方向表示は維持してよい。
- 回復量の追加調整は不要と判断した。
- 残リスクはv0.4以降へ送る。

## 2026-06-29: 最初のアイテム候補

決定: 最初のアイテム試作候補を `haste` とする。

理由:

- 開始、継続、終了が明確である。
- 統計とラン出力で確認しやすい。
- 攻撃力を直接上げない。
- 回復と役割が重なりにくい。

## 2026-06-29: 長期やりこみの順序

当初決定: v0.4で操作性、v0.5でステージ、v0.6で装備、v0.7でラン外進行を扱う。

現在の状態:

- 2026-07-08にv0.5をエンドレスの磨き込みへ変更した。
- 2026-07-10にビルドの個性をv0.6、戦闘展開とステージ試作をv0.7へ置いた。

当初の理由:

- 操作感が不安定なまま装備やラン外進行を入れると、調整原因が分からなくなる。
- ステージは障害物、ウェーブ、敵構成を束ねるため、装備より先に土台化する価値がある。
- 装備と解放は保存が絡むため、最初から大量実装しない。

## 2026-06-29: v0.4障害物の初回対応

決定: 中央障害物を削除し、跳弾は通常武器へまだ付与しない。

理由:

- 中央ブロックは開始位置付近の移動と射線を塞ぎやすい。
- 通常武器へ跳弾を付けると、バランス回帰テスト上の生存時間が大きく伸びた。
- 跳弾自体は有望なため、設定、データ、シミュレーション処理は残す。
- 後続で武器最終強化、ステージ特殊ルール、チャレンジとして評価する。

## 2026-06-29: 自動射撃試作

決定: プレイ中にマウス照準が確立されたら、Phaser入力アダプターで自動射撃する。

理由:

- `WASD + マウス照準 + 射撃維持` の同時操作負荷が高い。
- シミュレーションの入力境界を変えず、小さく試せる。
- Spaceキーを将来のダッシュまたは防御行動候補へ空けられる。

制約:

- メニュー操作中のマウス移動は照準として保持しない。
- 左クリックとSpaceキーの射撃は互換用として残す。
- バランス回帰テストは、人間にとっての入力改善を直接評価しない。

## 2026-07-01: v0.4から次フェーズへ進む判断

当初決定: 現在のバランスを即時変更せず、v0.4の操作方針を閉じた後にステージ基盤へ進む。

現在の状態:

- 2026-07-08にステージ基盤をv0.6へ移した。
- 2026-07-10に戦闘展開と合わせてv0.7へ移した。

理由:

- 手動プレイで約23000点のランが出たが、毎回再現した結果ではない。
- 従来の最高記録は約8000点で、通常は5000点未満が多かったため、突出値は上達余地として扱う。
- 最後は操作ミスで死亡しており、死因の納得感がある。
- 1回の高スコアだけで難度を変えると、操作慣れ、運、設定差を誤読しやすい。

制約:

- 高スコア、通常、短時間のランをラン出力で残す。
- 3本から5本の記録が揃うまでは、突出値だけで難度を変えない。
- 20000点以上が頻発し、緊張感が薄れる場合は再評価する。

## 2026-07-07: シードのランダム化と持久圧力

決定: ステージ基盤より先に、手動ランのシード変更と90秒以降の持久圧力を入れる。

理由:

- 自動ログで51282点、約882秒、レベル20の長時間ランを確認した。
- 強化後に長時間続けられそうな感触があった。
- ステージやボスを待たず、現在のアリーナでもラン差と終盤圧力を少し足す価値がある。
- 大幅な弱体化ではなく、シード差と小さな持久圧力に留める。

制約:

- 自動テストは固定シードを維持する。
- 手動ランの再現には `?seed=` を使う。
- 90秒以降の圧力には上限を持たせる。

## 2026-07-08: v0.5をエンドレスの磨き込みへ変更

決定: 次フェーズはステージ基盤へ直行せず、v0.5を「エンドレスの磨き込みとラン記録」とする。

理由:

- 現在のエンドレスが遊べる状態になり、リザルト、ランキング、履歴で振り返れる価値が高い。
- タイトル、HUD、演出、音を少し磨くと、次のプレイテストから得られる情報が増える。
- 外部ログインより先に、ローカルプロフィールとラン記録の境界を作る方がよい。
- ステージやボスへ進む前に、保存、表示、計測を固めると比較しやすい。

制約:

- 外部アカウント同期、オンラインランキング、バックエンドはv0.5で作らない。
- 無料BGMや効果音は、ライセンスと出典を確認してから取り込む。
- リザルトとランキングはゲームルール版ごとに分ける。

## 2026-07-10: ステージ追加よりビルドの個性を優先

決定: v0.5後はステージ基盤へ直行せず、v0.6を「ビルドの個性とエンドレス後半」、v0.7を「戦闘展開とステージ試作」とする。

理由:

- 51282点、約14.7分のランはレベル20、強化19回で、次のレベルに1792 XP必要だった。
- 全強化ランク24へ届くレベル25まで、現行式では累積約24088 XP必要になる。
- 開始武器が `pulse` 固定で、ラン開始時の方針が1つしかない。
- 90秒以降は敵構成の重みが同じまま、数値圧力だけが上がる。
- 同じアリーナでもビルド差が生まれる状態を先に作ると、後続ステージの価値を正しく評価できる。

制約:

- v0.5のラン記録、リザルト、ランキング基盤は予定通り先に閉じる。
- v0.6は開始武器、選択間隔、最初の武器最終強化へ範囲を絞る。
- ステージ、エリート、ボスは戦闘展開制御と出現安全規則の後に試す。
- アカウント同期、オンラインランキング、恒久ステータス周回はv1.0前の必須範囲にしない。
- ゲームルール変更時は `rulesetVersion` を分け、デバッグと自動テストを通常ランキングへ混ぜない。

## 2026-07-10: v0.4の終了と操作案の延期

決定: 自動射撃をv0.5の既定操作として暫定採用し、防御ダッシュ、右クリック固有スキル、跳弾の実戦投入はv0.6以降へ延期する。

理由:

- 自動射撃を含む現在の操作で、短時間から長時間まで複数の手動ランを完走できた。
- メニュー、強化、リザルトのカーソル問題は修正済みである。
- 防御行動と固有スキルを追加すると、v0.5の保存、画面、設定とは別のゲームプレイ調整が必要になる。
- 跳弾は通常性能へ入れると生存時間が大きく伸びる可能性があり、武器最終強化と合わせた方が役割を判断しやすい。

制約:

- 左クリックとSpaceはv0.5の手動射撃として残す。
- 自動射撃は `PH-V05-010` で切り替え可能にする。
- 延期項目は削除せず、v0.6以降のゲームプレイ試作として再評価する。
- 延期項目をv0.5の未完了条件へ含めない。

## 2026-07-10: v0.5を11チケットへ再編

決定: 既存9チケットに、設定とアクセシビリティ境界、安定化と手動プレイ判定を追加し、v0.5を11チケットで管理する。

理由:

- 音、揺れ、点滅、自動射撃の設定が複数機能へまたがるため、音響やプロフィールの付随作業にすると所有者が曖昧になる。
- 個別機能の完了だけでは、履歴の永続化、ランキング除外、長時間動作をまとめて判定できない。
- 開発用ランJSONは存在するが、プレイヤー記録、保存契約、プロフィールは未実装であり、実装済みと誤認しやすい。
- GitHub Issueが0件だったため、設計資料と実行管理の役割を明確に分ける必要があった。

運用:

- Starlightの [v0.5チケット詳細](../v05-tickets/) を要件の正本とする。
- [GitHubマイルストーン](https://github.com/garchomp-game/create-game/milestone/1) を担当、進捗、実装中の議論の正本とする。
- `PH-V05-001`を最初の実装チケット、`PH-V05-011`をフェーズ全体の最終判定とする。

## 2026-07-10: v0.5リリース候補とルールセット維持

決定: `PH-V05-001`〜`010`の実装と`PH-V05-011`の自動検証を完了し、v0.5をリリース候補とする。アプリ版は0.5、ゲームルール版は`phaser-v0.4-endless-pressure`を維持する。

理由:

- 記録、履歴、ランキング、プロフィール、設定、タイトル、HUD、演出、音響を接続した。
- 保存の一回性、破損、容量超過、件数上限、再読込、個別初期化を自動確認できた。
- 18状態の画面比較と900秒耐久シミュレーションが通った。
- 実装上の未解決P0は見つかっていない。

制約:

- 旧v0.4ログは`rulesetVersion`を持たないため、v0.5ランキング比較データへ移行しない。
- 900秒試験は加速シミュレーションであり、実時間GPU負荷と人間の操作感を代替しない。
- 15.1分の実時間ブラウザ耐久は完了した。v0.5履歴へ短時間、通常、長時間の3ランを残し、画面・音響の所感を確認してから`PH-V05-011`を閉じる。
- v0.6の設計準備は並行できるが、ゲームルール変更は手動ゲート後に始める。
- Scene / Rendererの追加分割とバンドル警告は、v0.6開始時に変更範囲と合わせて再評価する。

## 2026-07-10: v0.5音源はプロジェクト生成素材を使う

決定: 外部の無料素材を急いで採用せず、FFmpegで本プロジェクト向けに生成したBGM1曲と主要効果音8種をv0.5の基準素材とする。

理由:

- BGM制御、効果音ルーティング、音量、ミュート、ブラウザ音声ロックの基盤確認が主目的である。
- 外部素材のライセンス、再配布、クレジット条件を持ち込まずに検証できる。
- 外部素材へ差し替える場合も、`public/audio/README.md`を資産台帳として使える。

## 2026-07-11: v0.5完了と音響の再構成

決定: 通常UIからの手動3ラン、15.1分耐久、全自動検証、所感反映が揃ったため`PH-V05-011`を完了し、v0.5を閉じる。音響は外部曲へ急いで差し替えず、再生成可能な32秒BGMとSEバリエーションをv0.5採用品とする。

根拠:

- 約98秒、178秒、272秒の3ランが、同じv0.5ビルドのブラウザ履歴、ランキング、開発ログへ保存された。
- 3ランはすべて`manual`起点、ランダムシード、ランキング対象だった。
- 操作とUIは良好と評価され、初版SEの単調さが唯一の具体的な改善指摘だった。
- 射撃、命中、撃破、回収、被弾へ2〜3バリエーションを追加し、BGMを8秒から4区間・32秒へ再構成した。
- 18ファイル・121件の単体テスト、43件の通常E2E、18状態の画面比較、本番ビルドが再度成功した。

後続判断:

- 外部無料BGMは、ゲームプレイ拡張より優先しない。
- 採用候補が見つかった場合は、選曲比較とライセンス台帳更新を別チケットで行う。
- Scene / Renderer分割、バンドル警告、開始武器、XP曲線はv0.6へ送る。

## 2026-07-11: v0.6はPulseとSpreadの縦切りから始める

決定: v0.6は3武器と大量コンテンツの同時実装ではなく、PulseとSpreadの開始選択から、挙動変化型強化、リザルト比較までを先に通す。Pierceは固定配置と開発計測で役割を確認してから通常選択へ出すか判断する。

根拠:

- v0.5の手動5ランはすべてPulseで、SpreadとPierceの通常プレイ実績がない。
- 全5ランで`splitShot`がランク2、`vitalCore`がランク0だったが、強化の全提示履歴がなく原因を区別できない。
- 直近の約301秒と322秒の2ランは最終強化構成が同一で、選択によるラン差を追加計測する必要がある。
- ゲームデザイナー観点のレビューでは、コンテンツ量より「自分の判断でビルドが変わった体験」の不足が優先課題と評価された。
- 現在の手動ランは約98秒から322秒であり、最初の後半判断を6分以降へ置くと通常ランで検証しにくい。

制約:

- スコア差だけで武器の個性を判断しない。
- `PH-V06-008`で提示履歴、1射あたりの異なる命中敵数、移動・標的優先を計測する。
- `PH-V06-010`で貫通表示、偶数分裂、無敵時間中の敵弾処理を先に固定する。
- 最初の予兆付き危険イベントは120秒から180秒、危険・報酬契約はイベント採用後の約240秒を候補とする。
- v0.6の最終判定は`PH-V06-011`で行い、未採用試作は無効化または削除できる状態を保つ。

## 2026-07-11: v0.6の自動評価候補を固定

決定: XP上限140、Pulse反響回路の反射後寿命0.35秒、135〜165秒の射撃体集中、240秒の後半契約を手動評価候補として固定する。過負荷契約は標準外ルールとして通常ランキングから除外する。

根拠:

- PulseとSpreadを固定10シードで比較し、命中ボレー率、異なる命中敵数、被ダメージに役割差が出た。
- 反響回路0.12秒版は取得コストに見合わなかったが、0.35秒版は中央値441回発動、32回追撃し、生存、毎分スコア、毎分撃破が改善した。
- 射撃体集中は後半の同時敵数と敵弾数を押し上げ、予告・集中・回復を自動再現できた。
- 過負荷は敵速度1.12倍とスコア1.3倍を同時に与えるため、標準ランキングへ混ぜると比較条件が崩れる。
- 15分実時間試験で、危険イベント、標準契約、ビルド完成後を含めて性能・個体数上限を通過した。

制約:

- 自動入力の差だけで面白さを確定しない。
- PulseとSpreadを各2本以上手動プレイし、反響回路の地形利用、Spreadの距離管理、危険予告への反応を確認する。
- 手動評価で判断が増えない場合は、`features`で反響回路、射撃体集中、契約を個別に無効化できる状態を維持する。
- Pierce、Spread用最終強化、複数イベント、複数契約はv0.6へ追加しない。

## 2026-07-13: v0.6.1で成長継続と有限終了を両立する

決定: 通常ビルド完成後はEXレベルへ移行し、敵能力は脅威ティアで上限なく増やす。危険イベントを3種の反復デッキへ拡張し、600秒以降はアリーナ崩壊で安全領域を縮める。ルール版を`phaser-v0.6.1-endless-escalation`へ分ける。

根拠:

- 手動プレイで、全強化後に安定状態へ入り、その気になれば長時間生存できるという所感が得られた。
- 成長を完全に止めるとXP回収の意味と後半の選択がなくなる。
- 敵数と移動速度を無制限にすると、難易度より先に可読性と性能が破綻する。
- 敵HP・攻撃力の乗算成長、回復率低下、反復イベント、戦場収縮を組み合わせれば、密度上限を守りながら最終的な終了を作れる。
- 任意の瞬間死タイマーより、予告された安全領域の縮小の方が位置判断と死因の説明を残せる。

制約:

- EXの攻撃力と最大HPは線形、敵HPと攻撃力は乗算で成長させる。
- 連射、移動、敵速度、同時敵数、敵弾速度には安全上限を置く。
- 上限へ達した無効果のEX候補は表示しない。
- 初回イベント後の次予告は、240秒の契約選択と重ねない。
- 自動有限終了を面白さの証明には使わず、EX取得間隔、敵の硬さ、収縮の納得感を手動評価する。

## 2026-07-13: v0.6.2で共有敵経路と循環型EXを採用する

決定: 敵は射線が切れた場合だけ、プレイヤーを終点とする共有Dijkstra経路場で障害物を迂回する。EXは上限未到達候補をサイクルへ補充し、取得済み候補を一巡するまで戻さない。ルール版を`phaser-v0.6.2-navigation-extra-cycle`へ分ける。

根拠:

- 軸別衝突だけでは、敵が障害物の正面へ張り付き、地形を挟むことが恒久的な安全解になり得る。
- 敵ごとの毎フレーム探索は長時間・高密度ランの処理量に合わないため、目標セルと敵半径単位の共有場が必要である。
- 射撃体が遮蔽物越しに発射すると、地形の防御的な意味と表示上の射線が一致しない。
- v0.6.1の手動ログでは同じEX候補を連続取得しており、自由抽選だけでは取得順の偏りを抑えられない。
- 連射+2.5%と移動+1.5%を多数回取るより、+10%と+6%を各5段階にまとめた方が1回の選択結果を理解しやすい。

制約:

- 直線追跡で十分な敵まで経路探索へ入れない。
- 経路が得られない場合は従来移動へ戻し、シミュレーションを停止しない。
- 経路場は最近使った目標セルを最大64件に制限し、構築回数を診断ログへ残す。
- EX最後の1候補だけを自動取得し、2件以上残る場合はプレイヤー選択を維持する。
- ランダムな新能力はEXへ混ぜず、将来の変異、レリック、イベント、ボス報酬へ分離する。
- 自動入力のバランス許容内だけで採用を確定せず、包囲圧力とEXサイクルの理解を長時間手動ランで確認する。

## 2026-07-13: v0.6.3で開始武器の成長形状を分離する

決定: Pulseは0.12秒の単線射撃と集束共鳴、Spreadは0.33秒・広角3発から5発の分裂射撃と掃射循環へ分ける。240秒以降の敵HPは役割別に成長させ、選択画面はCanvas文字からDOMボタンへ移す。ルール版を`phaser-v0.6.3-weapon-identities`へ分ける。

根拠:

- Pulseが分裂射撃ランク2で3発になるとSpreadの初期形態を複製し、弾速、連射、反響を持つPulseが広角以外の価値を取り込みやすい。
- 単にPulseの弾数を減らすと180秒自動比較の生存中央値がSpreadから大きく離れたため、形状ではなく単線連射を0.12秒へ上げて総合差を補う必要がある。
- 10シード中央値で生存、毎分撃破、毎分スコアの差は20%以内、異なる命中敵数はPulse 1.20、Spread 1.60となり、総合性能と役割差を両立した。
- 全敵へ同じHP倍率を掛けると硬さだけが増えるため、Fastを柔らかく、RangedとBruteを集中処理対象として伸ばす方が武器役割と接続する。
- SVGをCanvasへ描いても動的な日本語は最終的にラスタライズされる。選択UIはブラウザフォントと実ボタンを使う方がDPI、カーソル、キーボード操作を一度に解決できる。

制約:

- Pulseへ弾数増加を戻さず、Spreadへ単体集中倍率を加えない。
- 集束は跳弾後に蓄積せず、反響回路を範囲補助として分離する。
- 掃射チャージは1つ、1斉射1回とし、次弾までの残り待ち時間だけを短縮する。
- 序盤の敵HPは変更せず、役割別倍率は240秒以降だけ適用する。
- 自動入力の20%差を面白さの完了条件にせず、両武器各2本以上の手動ランで標的優先と位置取りを確認する。

## 2026-07-14: v0.6.4でPulseの技能上限を弾速と外周反射へ置く

決定: Pulseへ分裂射撃を戻さず、加速弾を1ランク22%、最大約1405px/sへ伸ばす。反響回路は障害物または固定アリーナ外周で合計1回反射し、高速弾は移動線分上の連続衝突で処理する。ルール版を`phaser-v0.6.4-pulse-ballistics`へ分ける。

根拠:

- v0.6.3の手動Pulse 3本は4689点、8250点、6908点で通常ビルド完成へ届かず、分裂削除後の難度上昇が大きかった。
- 同じルールのSpreadは34434点、約421秒、通常ビルド完成277秒、EX Lv5 / C2へ到達し、広角処理の安定性が明確だった。
- Pulseへ分裂を戻すとSpreadの初期形態を再び複製し、開始武器の選択意味が薄れる。
- Pulseは直線上へ敵を並べる貫通、Brute / Rangedへの集束、障害物と外周の反射角を操作精度の報酬にできる。
- 最大弾速は50msで約70px進み、終点だけの判定ではFastを飛び越えるため、弾速調整と連続衝突判定を分離できない。
- 物理外壁にすると画面外出現と敵経路を変えるため、外周はPulse弾だけに作用する一方向フィールドが適切である。

制約:

- Pulseの0.12秒連射、集束共鳴、貫通数、Spread、敵HP、XP進行は変更しない。
- 飛行中の物理加速、分裂、撃破時破片は追加しない。
- 外周は敵、敵弾、プレイヤー、画面外32px出現、600秒以降の崩壊領域へ作用させない。
- 連続衝突判定によるSpread側の命中変化も10シード比較で監視する。
- 次のPulse 3ラン中1本以上が240秒から360秒で通常ビルド完成へ届くことを手動ゲートとし、未達ならPulse固有値を再評価する。
- 初回15分耐久で射撃体集中時の同時弾が408へ達したため、通常の攻撃頻度を変えず同時敵弾を256で制限する。発射待ちは蓄積せず、空きが出た後の一斉発射を防ぐ。
- 再耐久ではプレイヤー弾21、敵弾254、合計274となり、最大敵86、最大ヒープ72.1MB、終了60.64fps、Canvas非空、コンソールエラー0を維持した。

## 2026-07-14: v0.6を採用し、v0.6.5は計装だけに限定する

決定: Pulse 26355点ランを手動採否の成立とし、武器、敵、XP、反射回数を再調整しない。v0.6.5は反射成果の出所と実フレーム性能だけを追加し、ルール版`phaser-v0.6.4-pulse-ballistics`を維持する。600秒以降の崩壊体感は公開ベータの非停止観測へ移す。

根拠:

- Pulseは7972点 / 163.03秒の短いランと26355点 / 353.96秒の完成ランがあり、操作精度による振れ幅を確認できた。
- 完成ランは274.79秒で通常25ランクを終え、EX Lv3、3イベント各1回、240秒の標準契約へ到達した。
- 同ランは反射5374回、反射後命中1142回を記録したが、旧計測では障害物と外周のどちらが追撃へ寄与したか判別できなかった。
- 快適という所感と既存15分試験は良好だが、ランJSONにフレーム分位がなく、個別の実プレイを数値で説明できなかった。
- Phaserはゲームループの`rawDelta`と`actualFps`を既に提供する。Web Vitalsはページ体験、Long Tasks APIは50ms以上のメインスレッド占有を対象とし、現時点のフレームペーシング計測には追加依存が不要である。

制約:

- アプリ版だけを`0.6.5`へ更新し、ランキング比較条件を分けない。
- 障害物 / 外周別の追撃命中・撃破と、外周面別の追撃命中を旧記録互換で追加する。
- 平均、p95、最大フレーム時間、50ms超過数、推定FPS、Phaser実測FPSをラン単位で確定する。
- 1ランの成果帰属だけで壁反射を増減せず、公開ベータで複数ランを蓄積してから判断する。
- 公開ベータはゲスト / ローカル保存のエンドレスへ絞り、アカウント、オンラインランキング、ステージ進捗を前提にしない。

## 2026-07-15: 観戦AIは安全包絡内の領域巡回を試す

決定: 観戦AIの危険回避をすべての移動へ常時適用したまま、既存Policyが`patrol`を選ぶ空き時間だけを訪問履歴ベースの領域巡回へ置換する試験を行う。固定された四隅を順番に踏む方式は採用せず、現在の安全矩形を3 x 3へ分け、到達可能な内側地点を選ぶ。作業はv0.6から分離した`PH-AUTO-001`から`003`で管理する。

理由:

- 現在の周期`patrol`は訪問場所を記憶せず、フィールド端にXPが残る。
- 単一Pickupの毎フレーム比較だけでは、離れた密集地を計画的に掃除しにくい。
- 四隅は逃走方向が少なく、固定順路は安全性と武器比較の公平性を崩す。
- 既存のMovementは安全包絡を全Intentへ適用するため、回避と長期目標を別レイヤーとして扱える。
- 危険本番は敵数削減が将来リスクを下げるため、巡回と意図的XP回収を中断する方が一貫する。

制約:

- 安全な高価値XP、レベル直前、短いETA、35個超の密集回収は既存Policyのまま維持し、`patrol`が勝った場合だけ領域巡回する。
- 緊急回復は全フェーズで即時中断を許可する。
- 危険本番は既存の交戦と生存Intentを使い、経路上で偶然回収したXPだけを許容する。
- 危険予告と本番中は巡回用の経過時計を止め、終了直後の古い領域への突進を防ぐ。
- 崩壊領域、障害物、到達不能を毎回考慮し、訪問できない領域を強制しない。
- 巡回係数は武器とプロファイルで共通にし、`PH-BETA-001`とv0.7を原則として止めない。
- 先に比較スイッチとBaseline計測を追加し、`patrol`が全Decisionの1%未満なら挙動実装へ進まず要件を再検討する。

## 2026-07-15: 観戦AIの単純patrol置換を停止し、期限付き巡回候補へ改訂する

決定: `periodic-v3`の空き時間だけを置換する案は採用しない。`visit-history-v1`では、最終訪問から15秒で強まり30秒で最大になる3 x 3領域を`pending` / `recovery`かつ低圧時の巡回候補として既存Utility比較へ加える。50個超で安全な直接XP経路がない場合だけセル内XP密度も使う。既定は比較完了まで`periodic-v3`を維持する。

根拠:

- 開発24シード x 2武器、最大450秒のBaselineで、1秒以上の`patrol`を持つランは0 / 48だった。
- 全645167 Intentのうち`patrol`は445フレーム、0.069%で、事前停止条件1%を下回った。
- 同Baselineの平均地上XPは115.7個、最大336個で、空き時間の方向だけ変えても後半滞留へ届かない。
- 180秒同一シードの初期候補は、生存を維持しながら30秒窓訪問率と最大地上XPを改善し、危険中の巡回Intentを0に保った。

制約:

- 危険予告中の安全なXP、危険本番の交戦・生存、緊急時、35個超のXP backlogは巡回より先に確定する。
- 巡回Utilityは領域年齢と危険度だけから決め、武器と`fair` / `ceiling`で変えない。
- Movementの安全Tier比較は変更せず、停止が最安全なら非ゼロ移動を強制しない。
- 30秒で8 / 9領域へ入れるよう訪問半径を72pxとするが、画面端や固定四隅そのものを目標にしない。
- 24シード比較で無謀回収、回避可能な満HP回復、被ダメージ、XP / 分、生存の非劣性を満たさなければ既定へ採用しない。

## 2026-07-15: 訪問履歴巡回はopt-inで残し、既定採用を見送る

決定: `visit-history-v1`の実装と計測は検証専用として残すが、既定`periodic-v3`と`auto-pilot:tactical-observer-v3`を変更しない。開発ゲート不合格のため64シードホールドアウトへ進まない。

根拠:

- 初回24シード候補は生存+3.7秒、XP / 分+0.6%、30秒窓訪問率+1.7ポイントだった。
- 同候補は平均地上XP115.6→119.2、非active超過率70.6%→71.4%、敵弾被ダメージ301.6→331.4、回避可能な満HP回復3786→3992へ悪化した。
- XP密度fallbackと15秒から30秒のUtilityを入れた最終6シードは、最大地上XP299→278、非active超過率70.7%→69.8%、生存+7.9秒まで改善した。
- 最終候補も30秒窓訪問率73.0%で75%へ届かず、敵弾被ダメージ280.7→297.3へ増えた。
- 180秒単独計測は危険中巡回0、理由なし停止0.83秒、判断p95 1.424msで、構造と性能の問題ではなく回収成果と被ダメージのトレードオフが残った。

制約:

- opt-inランへ`auto-pilot:patrol:visit-history-v1`を付け、通常v3ランと混ぜない。
- 既定切替、v4 modifier、通常ゲームのルール版変更は行わない。
- 次に再開する場合は、安全XP候補の全分母、経路拒否理由、密度掃討の発動・取得成果を先に計測する。
- 人間プレイ、公開ベータ、v0.7の進行をこの実験で止めない。

## 2026-07-15: 第二次監査のP0を採用し、XPコリドーの既定化は保留する

決定: `report2.md`で再現された同一Pickupへの固執、候補見落とし、最終移動理由の不一致を修正する。部分速度、単一Pickup経路上のXP価値、連続backlog圧、予告時間予算も共通AIへ採用する。一方、`visit-history-v1`は引き続きopt-inとし、複数Pickupを束ねるTactical Taskと区間計測が開発ゲートを通るまで既定を`periodic-v3`から変更しない。

根拠:

- 近い到達不能3件の外側にある安全Pickupを見落とすことと、同一Pickupで0.35秒停滞解除が働かないことをマイクロケースで再現できた。
- 修正後の180秒スモークは平均地上XP26.22個、非activeの50超過率4.25%、未分類停止0、判断p95 1.254msだった。
- 3開発シード x 2武器、450秒では`visit-history-v1`の平均地上XP38.04個に対して`periodic-v3`は37.59個で、候補戦略が10%以上削減するゲートへ届かなかった。
- 同比較のXP / 分、生存、総被ダメージはほぼ同等であり、訪問履歴を削除する根拠も既定化する根拠もない。
- rot.jsと既存150pxグリッドで必要な経路評価を実装でき、新しいAIライブラリを導入する理由はない。

制約:

- 停滞対象は0.9秒除外するが、安全回避そのものを0.35秒で中断しない。IntentとMovementの安全包絡は分離したまま維持する。
- 50個は制御目標であり、危険イベント本番中まで厳密な上限にはしない。
- 現行の経路コリドー価値を、複数Pickupの2秒から15秒タスクが完成したものとして扱わない。
- 開発24シードのゲートを通る前に64シードホールドアウトを実行しない。
- AI入力の改善だけで武器や通常ゲームの数値を変更しない。

## 2026-07-15: Spreadを弱体化せず、Pulseの精密射撃報酬を上げる

決定: v0.6.6ではSpreadを変更せず、Pulseの基礎ダメージを1.16、集束共鳴を1段階+20%とする。同じ弾が跳弾前に複数敵を貫通した場合も、先に直撃した敵1体につき+20%を加える。連射速度、弾数、敵、XP、崩壊は変更しない。

根拠:

- 調整前の手動Pulseは77883点 / 632.81秒で崩壊1段階へ到達したが、敵58体を残し高速敵との接触で終了した。
- 同一3シード・各650秒の最終比較はPulse平均73608点 / 612.96秒、Spread平均83193点 / 648.32秒だった。Spread優位を残しつつ、スコア差13.0%、生存差5.8%で既存の20%基準内に収まる。
- Spreadの広角群れ処理は意図した扱いやすさであり、弱体化すると武器固有の爽快感を損なう。
- Pulseの連射をさらに速めると入力精度以外の処理量が増えるため、基礎単発と連続・貫通成功時の倍率へ報酬を置く。
- 最大集束時の単発は2.088、理論基礎DPSは約17.4で、最大弾数Spreadの約15.15を正確な継続照準時だけ上回る。

制約:

- 跳弾後命中は集束と貫通列倍率の対象外とする。
- 対象連続倍率と貫通列倍率は設定値を分け、旧ルール再現と将来の感度分析を可能にする。現行値はどちらも+20%とする。
- 対象連続と貫通列の強化命中数・追加ダメージも分け、手動ラン後に技能報酬の内訳を確認できるようにする。
- 90,000点は熟練手動プレイの到達目標であり、保証値や単一ランの必須合格条件にしない。
- 次の手動確認までは追加のPulse強化、Spread弱体化、敵軟化を重ねない。

## 2026-07-15: Pulse精密射撃を採用し、Phaser 4移行を分離する

決定: 92728点 / 672.20秒の手動Pulseランを`PH-V06-021`の完了根拠とし、追加の武器・敵数値変更を止める。次の作業はゲームルールを維持したままPhaserを3.90.0から4.2.1へ更新する。初回はCanvas互換を維持し、WebGL既定化は別ゲートにする。

根拠:

- 手動ランはEX Lv12 / C3、脅威Tier 10、崩壊Stage 2へ到達し、敵弾で有限終了した。
- 集束・貫通列の強化命中6711回、追加ダメージ3248.68から、狙った技能報酬が実ランへ寄与した。
- Phaser 4.2.1は正式安定版で、現行コードは独自Pipeline、Shader、Mask、DynamicTexture、旧Geometryを使っていない。
- シミュレーションと保存はPhaserアダプター外にあり、ランタイム更新とゲームルールを分離できる。
- CanvasはPhaser 4で非推奨だが、移行とrenderer変更を同時に行うと画像差・GPU差の原因を切り分けにくい。

制約:

- アプリ版だけを0.6.7へ更新し、ルール版`phaser-v0.6.6-pulse-precision-tuning`を維持する。
- 初回置換はCanvasと`roundPixels`を明示し、全E2Eと画像比較を通す。
- WebGLは同一ビルドの切替試験、モバイル画像、15分性能契約を通してから既定化する。

## 2026-07-15: Phaser 4のCanvas互換案を撤回し、WebGLを採用する

決定: 直前のCanvas先行案を実測結果で更新する。Phaser 4.2.1ではWebGLを既定rendererとし、Canvasは環境変数による診断用だけに残す。公開ベータの性能認証は、計測hook付きの最適化済みproductionビルドを`vite preview`で配信し、headed Chromeの実GPUで行う。

根拠:

- Phaser 4 Canvasでは標準`Text`が表示される一方、主要`Graphics`が欠け、全59件中14件の画像試験が実不具合で失敗した。
- 現行コードは独自Pipeline、Shader、Mask、Texture拡張を使わず、WebGLでは通常E2E 57件、画像比較25件、単体276件、本番ビルドが成功した。
- 画像比較だけ`?webglReadback=1`で`preserveDrawingBuffer`を有効にすると連続画面が安定した。通常E2E、耐久、本番は無効のままにできる。
- headless ChromeはSwiftShaderを使い、15分性能値が実端末を表さなかった。renderer名を検査してIntel UHD Graphicsへ分けると、dev耐久は全基準を通過した。
- 最適化済みproduction耐久は892.84秒、平均15.42ms、p95 29ms、50ms超過4 / 58583、Phaser実測32.35fps、終端外部31.56fpsだった。最大敵86、全弾257、ピックアップ980、最大ヒープ155.8MB、画面非空、エラー0も満たした。
- 同GPUのdev耐久は平均15.84ms、p95 34ms、50ms超過9件、終端31.69fpsで、productionは後半FPSを保ったままフレームの揺れを減らした。

制約:

- ルール版、保存形式、ランキング比較条件は変更しない。
- `roundPixels: true`を明示し、v3の座標丸めを維持する。
- test hookは耐久専用productionビルドだけでインストールし、通常公開ビルドでは`window`へ公開しない。
- hardware-soakは実GPU、production preview、他ブラウザ負荷なし、固定した電源・冷却条件で実行する。SwiftShaderを性能合否へ使わない。
- Phaser 4 WebGLで重大な端末不具合が見つかった場合はCanvasへ逃がさず、依存とアダプターimportを3.90系へ戻す。

## 2026-07-17: Pulse外周反射を公開ルールとして採用する

決定: Pulseの外周反射を有効のまま公開採用する。武器、敵、XP、進行の数値は変更せず、アプリ版を0.6.8、ルール版を`phaser-v0.6.8-pulse-boundary-ricochet`へ分ける。旧ルール記録は履歴へ残すが、新しいローカルランキングへ混ぜない。

根拠:

- 直近のPulse手動2ランは33069点 / 413.10秒、25493点 / 349.79秒、Spread手動2ランは25382点 / 354.78秒、33093点 / 416.72秒で、スコアと生存の到達帯が重なる。
- Pulseは外周反射7957回、外周追撃1332命中・625撃破を記録し、反響回路が実戦成果へ接続している。
- 外周はPulse弾だけに作用し、Spread、敵、敵弾、プレイヤー、敵出現、崩壊境界を変えない。Spreadの広角群れ処理を直接強化せず、Pulseの貫通順と復路射線だけを深める。
- 外周無効時は反響回路が4障害物だけに依存し、障害物配置を避ける局面でPulse最終強化の判断幅が薄くなる。
- 手動4ランは制御実験ではないため武器勝率の保証には使わず、公開採否は武器役割と実利用の成立だけで判断する。

制約:

- 外周反射の採用と同時に追加のPulse強化やSpread弱体化を行わない。
- HTML meta、タイトル、リザルト、RunRecord、詳細JSONでアプリ版、ルール版、build commitを追跡できるようにする。
- 公開版は端末内保存だけとし、詳細ランJSONを自動送信しない。開発ログAPI、debug hook、fixtureをproduction bundle検査で拒否する。
- 公開ベータの複数ラン分布で一方的な優位が確認された場合は、新しいルール版で再評価する。

## 2026-07-17: v0.6.8を公開ベータ基準として配信する

決定: commit `ff686f992a65`をv0.6.8公開ベータ基準とし、Cloudflare Worker `arena-core`のVersion ID `e86f90b8-ea15-4d1d-b01b-59e4f9fea78e`として配信する。Wave 0を完了し、以後のv0.7ルール変更はこの基準から分離する。

根拠:

- 単体試験は286件成功・2件skip、E2Eは64件成功・長時間耐久1件skip、Starlightは81ページをbuildできた。
- production artifactはapp `0.6.8`、ruleset `phaser-v0.6.8-pulse-boundary-ricochet`、build `ff686f992a65`で一致し、debug hook、fixture、開発ログAPI、source map、ログ、レビュー資料を含まない。
- 公開URLの通常UIスモークで、設定、ランキング、履歴、Pulse開始、自然終了、RunRecord、リトライ、一時停止、タイトル復帰、ベータ情報、第三者ライセンスを確認した。
- 公開RunRecordも三つの版情報が一致し、自動ブラウザ由来のランは意図どおりランキング対象外だった。
- ルートHTMLは`no-cache`、ハッシュ付きJSは1年`immutable`で、CSP、Permissions Policy、`nosniff`を配信している。
- `npm audit --audit-level=high`はHigh / Critical 0件で、既知のLow 1件はWindowsの開発サーバーに限られ、公開artifactへ直接影響しない。

制約:

- 公開ベータはゲスト、端末内履歴、端末内ランキングに限定し、アカウント、オンライン同期、解析、自動ログ送信を追加しない。
- production不具合の修正はv0.6.8のルールを変えない範囲に限定し、ルール変更時は新しいrulesetへ分ける。
- 公開ベータの複数ラン観測は継続するが、単一高得点や600秒以降の所感だけで公開版を先回りして調整しない。

## 2026-07-17: v0.7を技術previewとして採用する

決定: 最初のエクスペディション統合版をアプリ`0.7.0`、ルール`phaser-v0.7.0-first-expedition`へ分け、Cloudflare Version Previewへ公開する。v0.6.8 production trafficは変更しない。production昇格とv0.8着手はPulse / Spread各3本の人間プレイまで保留する。

根拠:

- 単体348件、Playwright 70件、画像31件、型検査、保存移行、Endless回帰が成功した。
- 3 seed x 2武器はすべて9分02秒から9分51秒で勝利し、最大敵35、総弾38、Pickup 124だった。
- 同一seed / inputのイベント列と終了world hashが完全一致した。
- 固定fixtureで構造化出現、突進、ボス2攻撃を両武器が回避でき、攻撃源をRun JSONまで追跡できる。
- 自動結果だけでは面白さと人間の技能負荷を判定できないため、production採用とは分ける必要がある。

制約:

- previewの自動ブラウザ操作を人間の手動ランとして記録しない。
- 手動採否前に追加コンテンツ、武器調整、カード時間調整を重ねない。
- 重大 / 高の再現不具合、不可避攻撃、両武器の到達不能があればproduction昇格を止める。
- 詳細ランJSONはローカル保存のままとし、アカウント、オンラインランキング、自動解析を追加しない。

## 2026-07-17: 初回遠征を学習ステージとしてRC2へ分ける

決定: `first-expedition`を物語上の最終面ではなく最初の実戦遠征と位置づけ、ルール版を`phaser-v0.7.0-first-expedition-rc2`へ分ける。RC1の手動2ランは調整根拠として保存するが、RC2のproduction採否ランへは混ぜない。

根拠:

- Pulseの手動勝利ランでは射撃イベントとボス直前が最も難しく、ボスは負ける要素が薄いという難度逆転があった。
- RC1はEndlessの90秒以降の出現圧力と240秒以降の敵成長をそのまま継承し、遠征カードがさらに物量を上げていた。
- 単純に敵を減らすと到達時のレベルと回復機会も失われたため、ステージ固有のXP、スコア、回復補正が必要だった。
- RC2の6ランは最大敵15から18の範囲で、レベル32から33、EX 6から7まで成長し、8:53から10:01で全勝利した。
- ボスの全ターンに予告付き射撃を置いても最大総弾37に収まり、同一seedの決定性を維持した。

制約:

- 現行ボスを最終ボス相当の弾幕へ引き上げない。高難度最終決戦は後続ステージへ分ける。
- ステージが1件の間は選択画面を増やさず、2件目を実装するときに進行保存と合わせて追加する。
- 勝利専用BGMは生成音源を使い、勝利時に敗北SEを重ねない。
- v0.6.8 productionとEndlessの設定、武器性能、記録は変更しない。

## 2026-07-17: RC2の学習ステージ判断を撤回し、第10最終遠征へ再分類する

決定: 現行エクスペディション縦切りを第1ステージではなく、第10ステージの`final-expedition`へ再分類する。ルール版を`phaser-v0.7.0-final-expedition-rc3`へ分け、RC2の学習ステージ判断は履歴として残すが現行仕様には使わない。

根拠:

- 初心者向けに敵を段階解禁しても、射撃イベントと中盤物量は第1ステージとして高難度だった。
- 一方でボス戦は道中より易しく、10ステージの最終面としては難度が逆転していた。
- 小型の黄緑高速体は小ささと速度で対処が難しく、大型の黄色重装体は大きく当てやすいため、従来の導入順は学習難度と一致していなかった。
- 有限ラン、Act、Encounter、Commander、Charger、ボスという縦切り自体は、最終ステージの基盤として再利用できる。

RC3の固定事項:

- 敵の初出順を`赤の追跡体 -> 大型の黄色重装体 -> 小型の黄緑高速体 -> 紫の射撃体`とする。
- 最終遠征では黄色重装体のHPを基礎3から実HP 8へ上げ、狙いやすい耐久目標にする。
- Commanderは最大HP 500とし、優先標的として専用HPを表示する。
- 最終ボスはHP 3400でプレイヤーを追跡し、通常ウェーブを止めず、予告付き広域射撃と挟撃増援を続ける。
- 第1から第9ステージは未実装とし、第1から第4で敵を1種ずつ学ぶ短い系列を先に設計する。
- 世界観と固有名詞は、四方侵入と2D障害物戦に矛盾しないことを確認してから別途決める。

制約:

- 高難度化を不可視攻撃、予告なし即死、無敵待ちで作らない。
- 自動probeは両武器の全勝を要求せず、双方の最終段階到達と各武器1勝以上を確認する。
- v0.6.8 production、Endless、武器性能、既存履歴は変更しない。

## 2026-07-18: EndlessのC5到達性と最終遠征の進行曲線を分離する

決定: v0.7 RC4でEndlessのEX必要XPを180開始、1.04倍、上限360へ変更する。第10最終遠征は従来の180開始、1.12倍、上限900をStage定義で保持し、RC3の難度と手動評価条件を変えない。ルール版は`phaser-v0.7.0-final-expedition-rc4`へ分ける。

根拠:

- 従来曲線のEndless固定6ランはすべてC3で終了し、C5が実質的に観測できなかった。
- C5はEX Lv17から始まり、新曲線では通常ビルド完成後の累計4,258 XPで到達する。
- 同条件の比較でC5以上は0 / 6から3 / 6へ増え、最大C6まで到達した。半数はC4以下で終了し、必到にはなっていない。
- 平均生存は658.34秒から683.34秒へ3.8%増えたが、全6ランが900秒前に終了し、脅威と崩壊の有限終了を維持した。
- 最終遠征の固定3 seed x 2武器は、Stage固有曲線の導入後も勝敗、終了時刻、スコア、密度がRC3と完全一致した。

制約:

- この変更と同時に武器、敵、回復、危険イベント、アリーナ崩壊の数値を変えない。
- C5到達率は観戦AIの回帰値であり、人間プレイの到達保証として扱わない。
- 最終遠征の手動採否はRC3から継続し、Endless曲線の変更を近道に採否しない。

## 2026-07-18: RC4を棄却し、最終決戦の位置変更と速攻得点をRC5へ入れる

決定: `phaser-v0.7.0-final-expedition-rc4`をproduction候補として棄却する。RC5では、近距離周回へ位置変更を要求する`command-pulse`、ボス戦中の雑魚由来回復を毎秒1個までにする決定論的制御、完遂15,000点とボス戦時間に応じた速攻ボーナスを追加する。回復全体、Endless、武器性能は変更しない。

根拠:

- RC3 / RC4の通常UI 5勝では、回復が総被ダメージの97.1%から99.5%を相殺した。
- ボスを中央へ寄せた周回は、貫通雑魚処理、回復供給、ボス火力を同時に成立させ、危険時だけ外側の蓄積Pickupへ逃げられた。
- Pulse手動3本はボス戦を長引かせるほど約330点/秒の雑魚得点を追加でき、早く倒す技能とランキング動機が逆転していた。
- 回復全体を下げると道中の回収経路と爽快感も損なうため、原因のあるボス戦へ変更を閉じる必要がある。
- RC5固定6ランは両武器とも勝利し、制圧衝撃波の範囲外・遮蔽回避、回復抑制、決定論を確認した。

制約:

- 制圧衝撃波は範囲外か障害物で必ず回避可能にし、画面外・予告なし・即死を使わない。
- 手動で従来周回を再試行するまでproductionへ昇格しない。
- 速攻得点はExpedition勝利だけへ適用し、Endlessの生存時間評価を変えない。

## 2026-07-18: UI再設計は既存境界を保ち、比較prototypeから採否する

決定: UI全面置換とフレームワーク先行導入は行わない。ゲームルールから独立した`presentation`、Phaser View、semantic DOM overlayの境界を維持し、`PH-V08-011`で画面別Presenterとtheme tokenを整える。`PH-V08-012`ではproductionから分離したTailwind / HTML草案を3案比較し、世界観チケット`PH-V08-010`の判断後に1経路だけをproductionへ移す。

根拠:

- simulationとdomainは既にPhaser非依存で、`ArenaScreenPresenter`とPassive Viewの分離も成立している。
- 戦闘予告はworldの重なり順と低遅延描画が必要でWebGLに適し、選択肢や将来のフォームは高解像度文字、focus、pointerを持つDOMに適する。
- TailwindのVite統合は静的CSSを生成するため、短期間の比較草案には使えるが、既存CSSを置き換える根拠にはならない。
- Lucideは既知操作のDOMアイコン候補になるが、戦闘エンティティとCanvas表現へ一律導入する必要はない。
- React / Vue / Svelteや重量級component libraryは、現在の状態量に対してbundleと二重lifecycleを増やす。

管理は要件・判断をStarlight、作業状態をGitHub Issues / Projectsへ一本化する。Linearや独自管理ツールは、二重更新を上回る不足が生じるまで追加しない。

## 2026-07-18: 戦術管制をUI基礎に採用し、ライブラリ導入を局所化する

決定: 同一データから作った3案×5画面の比較により、A「戦術管制」をproductionの情報階層と警告表現の基礎へ採用する。B「回収航路」は遠征進行と資源経路、C「精密アーケード」は武器成果とリザルトだけへ限定する。Tailwind CSSは独立prototype専用、Lucideは将来のDOMコマンド限定候補とし、現時点のPhaser production依存へ追加しない。

根拠:

- Aは四方侵入、障害物、危険予告、HP、進行の優先順をdesktopとportraitで最も安定して示した。
- BはXP、回復、ステージ継続を説明しやすいが、瞬間的な技能成果を伝える力が弱かった。
- CはPulse精度、Spread制圧、スコア更新を強く見せるが、戦闘HUD全体へ使うと通常情報と競合した。
- Tailwindの静的生成は草案比較を速めたが、既存`arena.css`と`ArenaTheme`を置き換える保守上の利益はまだない。
- 現行選択画面は文字主体で、Lucideを入れてもproductionの操作性や意味理解を直ちに改善しない。

最初のproduction縦切りは、`ArenaChoicePresenter`と入力callbackを維持したまま、武器・通常強化・EX強化・契約のPassive DOM Viewだけを採用方向へ置き換える。simulation、乱数、スコア、保存schema、ルール版は変更しない。

## 2026-07-18: 採用UIの最初の縦切りを選択画面へ限定する

決定: A「戦術管制」の情報階層を、武器・通常強化・EX強化・契約のsemantic DOM選択画面へ反映する。候補番号、役割、ランク、説明、取得後数値、選択コマンドを別領域へ置き、色とmarker形状を併用する。タイトル、戦闘HUD、リザルト、ゲームルールは同時に変更しない。

根拠:

- `ArenaChoicePresenter`と`ArenaChoiceOverlay`の境界が既にあり、simulationと乱数へ触れずに実画面を比較できる。
- 960 x 540の横比較と390 x 844の縦積みで、武器、通常、EX、契約の全種がoverflowせず、文字解像度とfocusを維持できた。
- Tailwind、Lucide、外部fontをproductionへ入れず、既存theme tokenとCSSだけで採用方向を表現できた。
- 固定6ランの勝敗、event hash、world hashがRC5基準へ完全一致した。

制約:

- RC5の人間プレイ採否が終わるまでproduction trafficを変更しない。
- 世界観、bitmap素材、fontは`PH-V08-010`のライセンス判断前に取り込まない。
- 次の画面を変更するときも、選択画面の変更と同じPRへ混ぜず、画像と入力契約を画面単位で固定する。

## 2026-07-18: 戦術選択では停止中の戦場と直前入力を保持する

決定: 通常強化、EX強化、後半契約では背景を透過し、desktopの候補を下段へ寄せて停止中の戦場を確認できるようにする。overlayへ自動focusし、`1`から`3`をTabなしで受け付ける。選択後は直前のpointer照準を保持するが、無敵時間や再開猶予は追加しない。

根拠:

- 不透明な全面選択では、再開直後の敵、弾、障害物、退路を確認できず、選択内容とは無関係な被弾が起きやすかった。
- PC専用の現行入力では、数字キーによる即決がpointer移動より短く、WASDから手を大きく離さずに選べる。
- 従来は選択状態へ入るとpointer照準を破棄し、自動射撃がpointerを動かすまで再開しなかった。
- simulationは選択中に停止済みであり、視認性と入力継続を直せば、バランスへ無敵規則を追加する必要がない。

制約:

- 背景は判断可能な明るさを残すが、候補本文とのコントラストを下げない。
- 武器開始選択は戦闘再開事故の対象ではないため、通常強化より強い背景遮蔽を維持する。
- 数字キー、pointer、Enter、Escapeの既存経路を競合させない。

## 2026-07-19: RC5を基準証跡とし、時計と記録をRC6で固定する

決定: RC5をproductionへ直接昇格しない。RC5の戦闘値、6勝probe、決定論、Preview smokeは基準証跡として保持し、RC6でEncounter時計、Commander期限、Expedition記録scope、ruleset分離を先に実装する。RC6のゲームルールbranchはUI変更前のRC5基点`d16655a`から分け、既存UI branchを維持する。

時計の所有者:

- `runElapsed`はRun Lifecycle / Worldが所有し、総クリア時間と配置期限へ使う。
- `actElapsed`はEncounter Directorが所有し、Act遷移へ使う。
- HUDはDirectorの状態を表示するだけで、run時間からActを独自計算しない。
- Commander cardはAct時計を止め、120秒はspawn成功後のactive時間だけで数える。
- spawn deferは決定論的に再試行し、配置期限で失敗させてAct時計を必ず解放する。

記録の所有者:

- Expeditionの主記録は勝利ランの総クリア時間とする。
- 速攻成果は時間メダルへ移し、戦術点から除外する。
- 敗北は履歴へ残すが勝利PBを上書きしない。
- 比較scopeを`overall | weapon`とし、`weaponId`をすべてのキーへ常時入れない。
- fixed runは実seed値ごと、random runはrandom同士で比較する。
- rulesetを`phaser-v0.7.0-final-expedition-rc6`へ更新し、RC5以前と混在させない。

根拠:

- HUDだけを固定しても、Directorが次Actを開始すれば表示と実状態が分離する。
- spawn前から120秒を数えると、出現上限などの配置都合をプレイヤー成績へ含める。
- 総クリア時間と速攻点の両方で順位を決めると、同じ技能を二重評価する。
- 総合PBと武器別PBは同じRunRecordから両立でき、常時weapon keyへ分割する必要がない。

正本は[最終遠征RC6の時計と記録規則](../../engineering/expedition-rc6-clock-and-ranking-adr/)とする。

## 2026-07-19: ボス回復をRC6の独立比較へ分ける

決定: 最終決戦の有限repair budgetは、時計と記録の修正へ混ぜず`PH-V07-012`でcontrol比較する。回復全体、Endless、武器数値は同時に変更しない。

回復相殺率は`ボス中の実HP回復量 / ボス中の総被ダメージ`と定義する。HP上限で無効になった回復やdrop表示値を分子へ含めず、総被ダメージ0の場合は算出不能とする。

根拠:

- RC3 / RC4では中央周回と雑魚処理だけで被害の97.1%から99.5%を相殺できた。
- RC5の毎秒drop上限と制圧衝撃波は局所対策として有望だが、人間の中央周回再試行が終わっていない。
- 時計、順位、回復を同時に変えると、クリア時間と戦術変化の原因を分離できない。

採用候補は同じ3 fixed seed x 2武器でペア比較し、両武器の到達性と手動戦術を確認してからRC6へ含める。

## 2026-07-19: 10本量産からStage 1 / 5 / 10の3作戦検証へ変更する

決定: `PH-V08-007`から`009`として計画していた10ステージ進行、第1から4、第5から9の一括実装を再スコープする。既存Issue #62 / #64 / #65を閉じて作り直さず、`PH-V09-001`から`003`へ改名し、Stage 1 / 5 / 10の3本だけを先に検証する。Stage 2から4、6から9はdeferredとしてIssue本文と設計文書へ残す。

役割:

- Stage 1: 赤の追跡体だけで移動、照準、回収、最初の勝利を学ぶ。
- Stage 5: 四方侵入から優先方向と退路を選ぶ複合判断を学ぶ。
- Stage 10: 現行最終遠征でCommander、Charger、追跡ボスを含む最終試験を行う。

根拠:

- Stage 10は高難度最終面として成立し始めたが、初心者学習と中間の橋渡しは未検証である。
- 10本を量産してから学習曲線の欠陥が判明すると、戦闘値、目的、背景、音、進行の手戻りが大きい。
- Stage 1 / 5 / 10で導線と難度差を検証すれば、残り7本の必要性と制作見積もりを実測できる。

10ステージ案は破棄せず、3本の採否後に実装、再設計、延期を改めて判断する。正本は[エクスペディション3作戦検証](../../design/expedition-campaign/)とする。

## 2026-07-19: v0.8は面白さの核を量産前に検証する

決定: RC6後は、危険反転、選択テンポ計測、技能shadow ledger、武器教義、最大密度の可読性、構造化プレイテストを依存順に行う。新しいライブラリや大量asset、残りstageを先行させない。

根拠:

- 現在の強みは、滑らかな操作、四方警戒、回避と照準、無限に伸びる技能差にある。
- 危険を避けるだけでなく敵へ返す仕組みは、Arena Core固有の短い攻防を作れる可能性がある。
- スコアだけでは上達理由が見えず、武器の役割も開始時だけでは薄くなりやすい。
- 最大密度の演出を先に増やすと、敵弾と危険予告の可読性を損なう。
- 開発者本人と観戦AIだけでは、初心者の学習性と再挑戦意欲を判断できない。

採否はIssue #76から#81で個別に行い、不採用要素をdataまたは表示境界から外せる状態を維持する。

## 2026-07-19: Commander配置を2秒再試行・10秒期限で固定する

決定: `PH-V07-010`ではCommander cardだけ`blocksActClock: true`とし、選択から撃破、`timeout`、`deployment-timeout`のいずれかまでAct時計を止める。構造化spawnは2秒間隔、10秒期限、初回を含む最大5回で固定する。120秒はspawn成功eventと同じ時刻から数え、未撃破時はCommanderを撤退させてランを継続する。

実装契約:

- Worldの`runElapsed`差分を、step開始時にblockされていない場合だけ`actElapsed`へ加える。
- `activeElapsed`はspawn成功まで0のままとし、成功後だけ進める。
- 期限と同時のstepではspawnを再試行せず、`deployment-timeout`を1回だけ記録する。
- Commander cooldownは600秒とし、Act停止で滞在時間が延びても同じActで再出現させない。
- Run exportへselected、deployment、spawn、終了時刻、試行回数、理由をDirector historyとして残す。

根拠:

- RC5実装はdefer前に`deployedCardKey`を確定しており、初回失敗後の再試行経路がなかった。
- run、Act、activeを同じ時刻で判定すると、配置都合が120秒へ混ざり、Commander中に次Actが始まる。
- 無制限再試行はsoft lock、即時失敗は一時的な敵上限を恒久失敗へ変えるため、短い有限再試行が必要である。

## 2026-07-19: RC6の主記録を勝利時間へ切り替える

決定: `PH-V07-011`でExpeditionの順位を勝利ランの総クリア時間昇順へ変更し、同時間だけ戦術点で比較する。敗北は履歴だけへ残す。比較問い合わせは`overall | weapon`を明示し、fixedは実seed値まで一致させる。rulesetは`phaser-v0.7.0-final-expedition-rc6`へ更新する。

Stage 10の時間メダルは金9分、銀10分、銅12分とする。完遂15,000点は総得点表示へ残すが、速攻による加点は0とし、戦術点は完遂bonus前のscoreとして保存する。

実装契約:

- RunRecordとstoreのschema versionは2を維持し、追加metricはdefaultで旧記録を読む。
- 保存ランキングはoverall上位と各weapon上位の和集合とし、一方のPBを保存上限で失わない。
- RC5以前は履歴へ残すが、ruleset queryでRC6のPB、順位、メダルへ混ぜない。
- 個別削除後は残る履歴と保存ランキングから順位を再構築する。

根拠:

- 総クリア時間と速攻点を同時に使うと、速さを二重評価する。
- `weaponId`を常時keyにすると総合PBを表せず、overallだけ保存すると武器別PBを失い得る。
- fixedという区分だけでは、異なる検証seedが同じPBへ混ざる。

## 2026-07-19: 2400 HP有限回復候補を棄却する

決定: `PH-V07-012`のcandidate Aとして、最終決戦だけに2400 HP・補充なしのrepair budgetを設定して比較した。controlは3/6勝利、candidate Aは0/6勝利だったため採用せず、RC6の既定値を`repairBudget: null`に維持する。

実装契約:

- pickup生成時に実際に供給するhealValueをbudgetから消費し、満HP取得、期限切れ、未回収でも返却しない。
- ボス中総被ダメージ、供給量、実回復、満HP取得、期限切れ、抑制理由、残量をcontrolとcandidateで共通計測する。
- candidate定義は切替可能な比較fixtureとして残すが、通常ランから参照しない。
- candidate値を結果に合わせて変更しない。phase遷移や攻撃回避による補充は別候補として事前登録する。

根拠:

- control勝利3本の相殺率はPulse / 20260719が1.000、Spread / 20260718が0.937、Spread / 20260719が0.963だった。candidateは順に0.814、0.782、0.788で、ボスHPを288、925、824残して敗北した。
- 完全同値だった敗北pairは供給253で終わったPulse / 20260718だけだった。ほかのcontrol敗北2本はcandidateが2400を使い切り、勝敗は同じでも戦闘値は同値ではない。
- 外周・遮蔽物への移動と通常敵撃破は残り、単純に回収行動を消した結果ではない。
- 自動中央周回近似は8.5秒で敗北し、手動で報告された誘導後の周回を再現しなかった。自動結果だけで欠陥解消済みとは判断できない。

詳細は[RC6 有限回復予算 比較結果](../../playtest/v07-rc6-repair-budget-report/)を正本とする。

## 2026-07-19: Expedition難度をAct進行へ同期し、自然勝敗と機構fixtureを分離する

決定: `PH-V07-008`で、Expeditionの通常wave、通常敵成長、遠距離攻撃、heal drop減衰を`runElapsed`ではなくDirectorのAct進行から導く`difficultyElapsed`へ統一する。総クリア時間、Commander期限、ボス固有攻撃は`runElapsed`を維持する。

RC6の6構成probeは、全6勝ではなく、全6のCommander撃破、Act 5、3攻撃種を機構到達性として要求する。boss phase 2の遷移と固有挙動は専用fixtureで保証し、自然runでは各武器1本以上のphase 2到達を要求する。自然勝利は修正前controlの3/6以上かつPulse / Spread各1勝以上を戦闘退行ゲートとする。CommanderのAct境界越えは専用fixtureで強制し、自然ランへ手加減入力を加えない。

根拠:

- #73後はCommander中に`actElapsed`だけが止まり、通常敵圧力がrun時間で先行していた。
- `actElapsed`をstepごとに丸める実装は30 / 60 / 120 / 144fpsで390秒境界を変えていたため、累積丸めを廃止した。
- 最終候補は修正前と同じ3/6勝で、Pulse 1勝、Spread 2勝。全6が全攻撃、5/6がphase 2へ到達した。
- ボス出現run時間は424.37秒から469.47秒だが、難度時計は392.533秒から394.167秒へ収まった。
- Pulse / seed 20260718は全攻撃を実行後、phase 1の開始12.83秒で接触死した。これを隠す難易度緩和は行わず、phase 2機構を専用fixtureへ分離した。
- Stage 10は高難度最終面であり、観戦AIの6/6勝へゲーム数値やAIを合わせると、人間難度と機構到達性を混同する。
- 人間向け難度、中央周回、回復循環は通常UIのPulse / Spread各1本で別に採否する。

実値と残ゲートは[RC6 統合QAレポート](../../playtest/v07-rc6-integration-report/)を正本とする。

## 2026-07-19: RC6をVersion Previewへ公開しproductionを維持する

決定: commit `f06c9b585cc4`をCloudflare Version `a9522576-e9ff-4fce-92df-35c9c732849c`としてuploadし、`https://v07-final-expedition-rc6-arena-core.garchomp-game.workers.dev`へ公開した。通常UIの実URLsmokeが完了するまで`wrangler versions deploy`は実行せず、productionはv0.6.8のVersion `e86f90b8-ea15-4d1d-b01b-59e4f9fea78e`を100%維持する。

実URLでは版情報、WebGL、設定、ランキング、履歴、Pulse自然終了と保存、リトライ、一時停止、ベータ情報、最終遠征選択、Spread開始をdebug hookなしで確認した。console / page error、失敗request、HTTP 4xx / 5xxは0件だった。残るproduction採否ゲートはPulse / Spread各1本の通常UI欠陥特化ランだけとする。

## 2026-07-19: RC6提出物再レビューのrelease contractを追補する

決定: `revise before adoption`の再レビューを受け、ゲーム数値を変更せず、code commit `c908450a7101`でrelease assertion、Expedition時間表現、Commander objective、敗北履歴を追補した。productionはv0.6.8のまま維持し、RC6は自動ゲート完了・通常UI手動ゲート未完了として扱う。

実装契約:

- repair release matrixは`allVictories`、`repairOffsetControlled`、`regularEnemiesRequired`、`arenaMovementRequired`を個別にassertする。
- Expeditionの保存、比較、時間メダル、精密表示、PB差分は整数centisecondを唯一のperformance表現にする。Endless v0.6.8の順位規則は変更しない。
- Commander active中だけ固有objectiveを表示し、撃破、active timeout、deployment timeout後は現在Actの通常objectiveへ戻す。
- Expedition敗北の履歴にも`PB対象外: 遠征未完遂`を表示する。
- code SHAでは65 files・420 passed / 2 skipped、normal / repair probe各`1 passed / 1 skipped`、Playwright 73 passed / 1 skipped、production buildを通過した。

production採否には、同じVersion PreviewでPulseの中央周回とSpreadの通常戦術を各1本、両武器ともboss phase 2と3攻撃種まで確認する手動ゲートを残す。debug時刻ジャンプと将来fallbackの共通API edge、厳密な永続LRU、GitHub Actions新設はRC6の直接blockerにせず、再現条件と着手条件を後続で管理する。

## 2026-07-20: 通常UI採否を通過しRC6 controlを採用する

決定: `faeb3f5e8c5e`のVersion PreviewでPulse 2本、Spread 1本の欠陥特化ランを行い、`repairBudget: null`のRC6 controlをゲームルール候補として採用する。2400 HP有限回復candidate Aは棄却したまま再調整しない。Draft PR #82をmainへ統合し、UI candidateとproduction traffic切替は別判断にする。

証拠:

- Pulseは430.66秒 / 44,574点でphase 1敗北後、578.93秒 / 107,011点でphase 2勝利した。
- Spreadは584.49秒 / 108,891点でphase 2勝利した。
- 両武器で`targeted-salvo`、`escort-pincer`、`command-pulse`を確認した。
- 勝利ランの回復相殺率は99.96%と98.21%だったが、位置、標的、回復経路を変えない中央周回には固定できなかった。
- 範囲外退避、障害物、通常敵撃破、回復取得、ボスへの再接近を組み合わせる必要があり、回復をさらに減らすと終盤密度へ過度に厳しくなると判断した。
- 不可視攻撃、操作不能、soft lock、記録損失、P0 / P1不具合、50ms超フレームは確認されなかった。

高い回復相殺率だけを欠陥判定に使いません。相殺率は供給量の観測値であり、無期限の安定循環には「位置、標的、回復経路を変えずに120秒以上維持できる」という行動条件も必要です。今回その条件は成立しませんでした。

## 2026-07-20: 外部ゲームデザイン助言をv0.8設計入力として扱う

決定: 緊張と緩和、HP以外のボス強度、攻略メタ、惜敗、再挑戦支援に関する外部助言を[外部ゲームデザイン助言メモ](../../design/external-game-design-advice/)へ原文保存し、[PH-V08-020 #83](https://github.com/garchomp-game/create-game/issues/83)でWork再レビューと設計契約化を行う。RC6採用とPR統合はこのレビューで止めず、v0.8のゲームルール実装前に結果を反映する。

公平性の境界:

- Endless、fixed seed、ランキング対象ランへ、履歴に応じた隠れた難度補正を入れない。
- Campaign Assistを導入する場合は明示選択とし、difficulty、assist、rulesetをRunRecordへ残す。
- Assist使用ランを標準ランキングへ混ぜない。
- 「自分が上手くやった」という感覚は、判定の偽装ではなく、成功した判断と前回差の可視化で支える。

Work回答は設計入力であり、採用・保留・棄却はIssue #83と本decision logへ記録する。10ステージを一括量産せず、Stage 1 / 5 / 10とEasyからHellの難度軸を設計上分けて検証する。

## 2026-07-20: Encounterの期限切れ要求と予告外fallbackを共通APIで拒否する

決定: `PH-QA-002`で、telegraphからdeployment deadline以後へ時刻が飛んだ場合は配置要求を発行する前にtimeoutへし、fallback geometryは元の予告方向集合を増やさない場合だけ実行する。通常のEncounter数値、Commander、Boss、rulesetは変更しない。

根拠:

- 通常の0.05秒stepではdeadline判定が先に走るが、debug相当の大幅な時刻ジャンプではtelegraph分岐から期限切れrequestを1件出せた。
- 現在のfallback指定は予告方向の部分集合だが、共通APIは将来callerが`arc`から`pincer`へ切り替えて予告外方向を増やすことを防いでいなかった。
- 再予告の状態機械を追加する必要はなく、予告集合を増やさないfallbackだけを許可すれば現行用途を維持できる。
- 厳密な永続LRUはこの問題と独立し、現行仕様もranked record最新時刻による上限と明記済みのためschemaを増やさない。

検証: Director、Controller、structured spawnの専用fixture、65 files・424 passed / 2 skipped、型検査、配布build、release smoke 6件、Starlight 92ページを通過した。RC6 normal probeは従来と同じ3/6勝、両武器各1勝以上、同一event / world hashを維持した。
